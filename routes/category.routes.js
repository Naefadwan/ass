const express = require("express");
const router = express.Router();

// TODO: attach your /products endpoints here, e.g.:
// router.get("/", getProducts);
// router.post("/", protect, authorize("admin"), createProduct);

module.exports = router;
